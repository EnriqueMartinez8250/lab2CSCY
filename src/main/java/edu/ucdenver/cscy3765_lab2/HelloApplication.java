package edu.ucdenver.cscy3765_lab2;

import jakarta.ws.rs.ApplicationPath;
import jakarta.ws.rs.core.Application;

@ApplicationPath("/api")
public class HelloApplication extends Application {

/** TODO-1: [DONE]
 * - Install tomcat if you haven't done that.
 * - Make sure you update the tomcat run configuration to the proper path.
 * - do not remove the todos tags. After completed, replace the [TO DO/DONE] with [DONE]
 *
 * The changes will be given by: a) authenticate using username, password, and 2FA pin. b) signup needs to generate the
 * 2FA QR code that will generate the certificate for the user. c) no data will be persisted.
 *
 * You can use any TOTP app on your mobile to generate the pins.
 * We tested it out with Google Authenticator.
 * Let us know if you use another app and how it goes!
 *
 *
 */
}

